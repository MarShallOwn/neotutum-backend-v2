

export enum ENTITY_STATUS {
    DRAFTED = 'drafted',
    CHANGED = 'changed',
    COMMITTED = 'committed',
    ARCHIVED = 'archived',
    DELETED = 'deleted'
}

export enum RISKOBJECT_TYPES {
    VIRTUAL = 'virtual',
    PHYSICAL = 'physical',
    MODEL = 'model'
}